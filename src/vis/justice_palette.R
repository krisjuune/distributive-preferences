# Shared colour scheme for the 3 justice-profile classes, sourced by every
# LPA plotting script so a class always has the same colour everywhere.

justice_palette <- c(
  Egalitarian = "#c6461f",
  Universalist = "#007b97",
  Utilitarian = "#f59a00"
)
